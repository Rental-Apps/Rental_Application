@page
@model Rental_Application.Web.Pages.Login.VerifyLoginOTPModel
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GTL Infrastructure</title>
    <!-- jQuery UI -->
    <link rel="stylesheet" href="../src/css/jquery-ui.css">
    <link rel="stylesheet" href="../src/fonts/stylesheet.css">
    <link rel="stylesheet" href="../src/css/bootstrap.min.css">
    <!-- Custom styles for this template -->

    <link href="../src/css/style.css" rel="stylesheet">


</head>
<style>
    body {
        background-color: #52377c47; /* Light background color */
    }

    .login-form-main-bg {
        background-color: #fff; /* White background for the form */
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Shadow around the form */
        margin-top: 20px;
        width: 400px;
    }
</style>

<body>
    <div class="container d-flex justify-content-center align-items-center" style="min-height: 100vh;">
        <div class="login-form-main-bg">
            <div class="w-100">
                <form method="post" class="login-form-main" style="margin-top: 1rem !important; margin-left: 50px; margin-right: 50px; margin-bottom:20px;">
                    <div class="mb-4 text-center">
                        <h4>Verify OTP</h4>
                    </div>
                    <div class="row mb-3">
                        <label for="inputEmail3" class="col-form-label">OTP</label>
                        <div class="col-sm-12">
                            <input type="text" class="form-control form-control-lg" id="otp" asp-for="Otp" required>
                        </div>
                    </div>
                    <div class="d-grid mt-4">
                        <button class="btn btn-primary btn-lg" id="btn_Verify" type="submit">Verify OTP</button>
                    </div>
                </form>
            </div>
        </div>
    </div>






    <script src="../src/js/jquery.min.js"></script>
    <script src="../src/js/bootstrap.min.js"></script>

    <!-- jQuery UI -->
    <link rel="stylesheet" href="../src/css/jquery-ui.css">
    <script src="../src/js/jquery-ui.min.js"></script>
</body>

</html>

@* <script>
    $(document).ready(function(){
         $('#btn_Verify').on('click', function(event){
             event.preventDefault(); // Prevent default form submission if necessary
             alert("abc");
             post();
         });

         function post() {
             alert("abc");
             // e.preventDefault(); // Commented out; already used in the click handler if needed

             var recipientEmail = "narayanbhati1261996@gmail.com";
             var subject = "test mail";
             var body = "sample mail";
             var otp_code = '235689'; // $('#otp').val();
             var loginId = 'GIL000000897';

             alert(loginId);

             $.ajax({
                 type: "POST",
                 url: "https://localhost:7036/api/Auth/VerifyOTP",
                 data: JSON.stringify({
                     loginId: loginId,
                     otp_code: otp_code
                 }),
                 contentType: "application/json",
                 success: function (data) {
                     //alert("OTP verified successfully");
                     window.location.href = '/TicketLising';
                 },
                 error: function (xhr, status, error) {
                     alert("Error: " + error);
                 }
             });
         }
    });
</script> *@
